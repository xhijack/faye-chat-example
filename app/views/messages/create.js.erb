<% broadcast "/messages/new" do %>
$(".chatroom").append("<%= escape_javascript render(@message) %>");
var mydiv = $('.chatroom');
mydiv.scrollTop(mydiv.prop('scrollHeight'));

<% end %>
$("#new_message")[0].reset();